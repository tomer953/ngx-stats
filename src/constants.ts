export const FILE_EXTS = [".ts"];

export const IGNORE_DIRS = new Set<string>([
  "node_modules",
  "dist",
  "build",
  "cache",
]);

export const IGNORE_FILE_PATTERNS = [".stories.ts", ".spec.ts", ".d.ts"];
